<%- include("../partials/header") %> <%- include("../partials/nav") %>

<main class="container py-4">
  <div class="d-flex justify-content-between align-items-center mt-4 mb-5">
    <h1>MON PROFIL</h1>

    <nav class="w-25">
      <ul class="profile__menu d-flex justify-content-around bg-primary">
        <li><a class="text-white" href="/profile">Profil</a></li>
        <li>
          <a class="active text-white" href="/profile/quizzes">Mes quizs</a>
        </li>
        <li><a class="text-white" href="/profile/score">Mes scores</a></li>
      </ul>
    </nav>
  </div>
  <h2>Questions du quiz : <%= quiz.title %></h2>

  <% if (locals.error) { %> <%- include("../partials/error") %> <% } else { %>
  <ul class="mb-4">
    <% for (const question of quiz.questionsList) { %>
    <a class="text-decoration-none" href="/profile/quiz/<%= quiz.id %>/question/<%= question.id %>/edit">
      <li class="quizzes bg-primary p-4 mb-2 text-white">
        <div class="d-flex justify-content-start align-items-start flex-column">
          <h3><%= question.description %></h3>
        </div>
      </li>
    </a>
    <% } %>
  </ul>
  <% } %>
  <a class="button" href="/profile/quiz/<%= quiz.id %>/questions/add">Ajouter une question</a>
</main>

<%- include("../partials/footer") %>
